image: "continuumio/miniconda3:latest"

before_script:
  - python --version
  - pip install -r requirements.txt
  
stages:
  - Static Analysis
  
flake8:
  stage: Static Analysis
  script:
    - flake8 ./*.py
    - flake8 ./core_classroom_detection/*.py